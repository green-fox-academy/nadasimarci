'use strict';

// Create a program that prints all the even numbers between 0 and 500

for (let evenNumbers = 0; evenNumbers <= 500; evenNumbers++) {
   if  (evenNumbers%2 == 0) {
    console.log(evenNumbers)
   }
}